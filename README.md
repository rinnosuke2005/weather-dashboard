# Weather Dashboard（天気予報ダッシュボード）

Open-Meteo APIを活用し、指定した都市の天気情報を**時系列チャートで直感的に確認できるSPA（Single Page Application）**です。

---

# 1. 起動手順

ローカル環境で実行するための手順です。

## 前提条件

- Node.js（v18以降を推奨）
- npm または yarn

## セットアップ

```bash
# リポジトリをクローン後、依存パッケージをインストール
npm install

# 開発サーバーの起動
npm run dev

起動後、ブラウザで以下にアクセスしてください。

http://localhost:3000


2. 技術選定理由
フレームワーク

Next.js (App Router) / React

モダンなReact機能を活用し、コンポーネント指向で保守性の高いUI構築を行うため採用しました。

データフェッチ

SWR

要件である 「無駄な再フェッチ / 再描画の抑制」 を満たすために採用しました。

フォーカス時の自動再検証

強力なキャッシュ機能

これにより タブ切り替え時のUXが大幅に向上します。

スタイリング

Tailwind CSS

ユーティリティクラスを利用し、

グラスモーフィズム

モダンUI

などの複雑なデザインを高速かつ一貫して実装できるため採用しました。

チャート

Recharts

Reactとの親和性が非常に高く、

レスポンシブ対応

カスタムTooltip

カスタムドット

などの実装が柔軟に行えるため採用しました。

3. ディレクトリ構成

関心の分離（Separation of Concerns） を意識し、
UIとロジックを明確に分離しています。


├── app/
│   ├── globals.css      # 全体スタイル
│   ├── layout.tsx       # ルートレイアウト
│   └── page.tsx         # メインページ（各コンポーネントの統合）
│
├── components/          # UIコンポーネント
│   ├── ControlPanel.tsx # 操作パネル（都市・指標・期間・単位の選択）
│   └── WeatherChart.tsx # グラフ描画コンポーネント
│
└── hooks/               # カスタムフック（ビジネスロジックの分離）
    ├── useWeatherData.ts     # SWRを用いたAPI通信とデータ整形
    └── useWeatherSettings.ts # LocalStorageを利用した状態・設定管理


4. 工夫点
アーキテクチャ設計（カスタムフックの活用）

UIコンポーネントの肥大化を防ぐため、

API通信 → useWeatherData

状態管理 → useWeatherSettings

としてカスタムフックに分離しました。

これにより

コンポーネントの責務が明確

再利用性向上

可読性向上

を実現しています。

アクセシビリティ（a11y）

マウス操作だけでなく、

Tabキー

矢印キー

などキーボード操作のみでもすべての操作が可能です。

また

fieldset

sr-only

フォーカスリング

などを利用し、スクリーンリーダー対応も考慮しています。

UX / UIの向上

以下の改善を行いました。

モダンUI

グラスモーフィズムを採用

背景に溶け込むデザイン

設定の保存

ユーザーの設定を localStorage に保存し、

再訪問時に復元

Next.js特有のハイドレーションエラーにも対応

しています。

グラフの視認性改善

7日間表示の場合、

X軸ラベル

ドット表示

が密になりすぎないよう、

12時間間隔で間引くカスタムロジック

を実装しています。

生成AIの活用

開発スピードと品質向上のため、
生成AIをペアプログラミングのパートナーとして活用しました。

主な用途：

要件定義の壁打ち

UIデザインのブレインストーミング

Tailwind CSSのクラス構築

最終的な

アーキテクチャ設計

アクセシビリティ

については自身で検証しながら実装しています。

5. 既知の制約
Rechartsの型定義に関する警告

Rechartsの一部コンポーネント（例）

Tooltip

CustomDot

では、ライブラリ側の型定義の都合上
ESLintの厳密な型チェックを一部意図的にバイパスしています。

ブラウザ上の実際の動作には影響ありません。

極小画面でのツールチップ表示

画面幅が非常に狭いスマートフォン端末（例：320px以下）で
7日間グラフを表示した場合、

ツールチップが画面端でやや見切れる可能性があります。